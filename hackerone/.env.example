# HackerOne API Credentials
# Required: Get these from https://hackerone.com/settings/api_token
HACKERONE_API_USERNAME=your_username  # Your HackerOne API username
HACKERONE_API_TOKEN=your_api_token    # Your HackerOne API token

# Rate Limiting Settings
# Optional: Adjust these if you need to modify default rate limits
#HACKERONE_READ_RATE_LIMIT=600    # Max read requests per minute (default: 600)
#HACKERONE_WRITE_RATE_LIMIT=25    # Max write requests per 20 seconds (default: 25)
#RATE_LIMIT_BUFFER=0.9            # Safety buffer for rate limits (default: 0.9)

# Logging Configuration
# Optional: Control logging behavior
#LOG_LEVEL=INFO                   # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
#LOG_FILE=hackerone_reports.log   # Log file path (default: stdout)
#LOG_FORMAT=%(asctime)s - %(levelname)s - %(message)s  # Log message format

# Report Settings
# Optional: Configure default report behavior
#DEFAULT_SEVERITY=medium          # Options: none, low, medium, high, critical
#DEFAULT_WEAKNESS_ID=null         # Default CWE ID (null for none)
#ATTACHMENT_MAX_SIZE=10485760     # Max attachment size in bytes (10MB)
#MAX_RETRIES=3                    # Max retry attempts for failed requests

# Notification Settings
# Optional: Configure notification channels
#SLACK_WEBHOOK_URL=               # Slack webhook for notifications
#EMAIL_NOTIFICATIONS=false        # Enable email notifications (true/false)
#NOTIFICATION_LEVEL=ERROR         # Minimum level for notifications

# Proxy Configuration
# Optional: Set if behind a proxy
#HTTP_PROXY=                      # HTTP proxy URL
#HTTPS_PROXY=                     # HTTPS proxy URL
#NO_PROXY=                       # Comma-separated no-proxy list

# Development Settings
# Optional: For testing/development
#ENV=production                   # Options: development, testing, production
#DEBUG=false                      # Enable debug mode (true/false)
#MOCK_API=false                   # Use mock API for testing (true/false)
